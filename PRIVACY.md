# WebFile Hunter 隐私政策

生效日期：2026-07-28

WebFile Hunter 默认完全在用户的 Microsoft Edge 浏览器本地运行。

## 数据处理

- 扫描结果、文件链接、文件名、任务历史、下载任务和设置保存在本地浏览器的 IndexedDB 或 `chrome.storage`。
- 文件链接只在用户启动递归扫描、主动获取元数据或手动开始下载时访问。
- 扩展不上传网页内容、URL、文件名或扫描历史。
- 扩展不使用第三方分析、广告 SDK、静默遥测、账户或云同步。
- 扩展不出售、出租或共享用户数据。

## 不收集的数据

- 不读取或保存密码输入框。
- 不读取用户填写的表单内容。
- 不收集 Cookie、Authorization 头、登录凭据或 Token。
- 不查询浏览历史；只在界面运行时读取当前活动标签页的 URL 与标题以显示上下文，实时监听只处理用户当前指定的标签页。

## 网站访问

所有扫描只在用户点击后运行，并按当前站点请求可撤销的可选主机权限。递归扫描只访问用户授权的同源 HTTP(S) 公开页面，不绕过登录、付费墙、防盗链、验证码、许可证或 DRM。

## 用户控制

用户可以在设置页：

- 撤销任意已授权网站；
- 停止、暂停或取消任务；
- 删除单个扫描会话；
- 清除全部本地数据。

撤销网站权限后，对应的当前页、监听或递归任务会安全停止。

## 遥测

当前版本不包含遥测。未来若增加遥测，必须单独提供清晰、默认关闭的选择加入机制，并更新本政策。

## 联系与报告

隐私或安全问题请通过本仓库的安全报告渠道提交。不要在公开问题中粘贴含签名参数、私人网址或其他敏感数据的扫描日志。
