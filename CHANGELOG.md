# 更新记录

## 1.3.0 - 2026-08-04

- 新增独立“文本”侧栏，提取当前页、权限允许的 frame 与同源递归 HTML 的公开可见正文，支持页面切换、搜索计数、复制和 TXT 导出。
- 正文明确排除显式隐藏内容、脚本样式、模板、输入框、密码、文本框、下拉选项和 `contenteditable` 草稿；内容只在本地按纯文本渲染，不执行 OCR。
- IndexedDB 升级至 v2 并新增正文存储；单页限制 200,000 字符，单任务限制 500 页/5,000,000 字符，任务删除、过期清理、历史清空和全部清理均同步移除正文。
- 资源发现补充 `data-srcset`、`data-lazy-src`、背景/缩略图/文件 URL 数据属性以及 SVG `use`、`feImage` 引用，动态监听同步观察新增属性。
- 动态资源批次不再重复发送整页正文；文本更新改为用户明确重新提取，减少复杂 SPA 上的消息体积与本地写入。
- 六栏导航、文本阅读区、窄屏换行、键盘焦点、状态反馈和 reduced-motion 完成 UI/UX 回归；继续默认静音，不播放音效或背景音乐。
- 真实 Edge E2E 覆盖正文、开放 Shadow DOM、跨域 frame、递归页面、隐私过滤、搜索、复制、导出、280/320/380px 六页侧栏与级联清理。

## 1.2.0 - 2026-08-03

- 新增 JSON-LD 明确资源字段、显式 MIME、itemprop 与 `rel=enclosure` 发现，避免把普通结构化页面 URL 当作下载资源。
- 当前页扫描进入 `<template>` 与开放 Shadow DOM，并能在实时监听启动后发现新附加的开放 Shadow Root 及其中样式资源。
- 无扩展名的明确资源获得可见置信度；图片关闭时统一过滤延迟图片、poster、SVG image 与结构化图片语义。
- Shadow/Template 根发现增加 20,000 元素与 1,000 root 硬上限，避免超大 DOM 的持续监听造成无界遍历。
- 真实 Edge E2E 覆盖 JSON-LD、显式 MIME、template、开放/延迟 Shadow DOM、递归、导出、下载与五页窄屏界面。
- 扩展继续完全本地运行并保持默认静音，不播放音效或背景音乐。

## 1.1.1 - 2026-08-02

- 修复 SPA 修改既有 Open Graph、Twitter Card 与 itemprop 元信息属性后未触发重新扫描的问题。
- 递归 DOM 种子尊重页面级 robots `nofollow`/`none`，并跳过明确使用 POST 或 dialog 的表单 action。
- 修复 robots.txt 在 `Crawl-delay` 后错误合并新 User-agent 分组的问题。
- 外域元数据探测识别已授予的对应资源站点权限，并为禁用状态显示明确授权原因。
- 损坏的 `filename*` 不再覆盖合法 `filename`；URL 检查补充 CGNAT、文档、基准、组播及 IPv6 特殊地址。
- 应用快照并行读取结果、设置、权限、下载与未完成任务，降低侧栏刷新等待。
- 真实 Edge E2E 新增动态 Open Graph 属性更新，并继续覆盖第三方网络响应、跨域 frame、递归、导出、下载与五页窄屏界面。
- 扩展继续完全本地运行并保持默认静音，不播放音效或背景音乐。

## 1.1.0 - 2026-08-02

- 递归页面改为直接 GET，不再因站点拒绝 HEAD 而跳过 HTML；临时限流与网络错误继续遵守重试、退避和取消配置。
- 读取 robots.txt 声明的 Sitemap/Sitemap Index 和 raw gzip Sitemap，将未出现在页面链接中的同源公开页面加入受限队列。
- 启动递归时扫描当前标签页已渲染 DOM，为 SPA 客户端路由补充同源页面种子。
- 补充 video poster、SVG image、`data-poster`、Open Graph、Twitter Card 和 itemprop 资源发现，过滤 canonical/preconnect 等非资源 link 噪声。
- 支持按响应头、BOM 与 meta 声明解码 GBK 等非 UTF-8 HTML；robots.txt 与 Sitemap 复用可取消的重试策略。
- 修复网络来源页路径丢失、全站权限下外域元数据按钮误禁用、Popup 权限手势顺序、全失败假完成及恢复任务成功计数丢失。
- 删除未生效的扫描配置字段，统一运行时配置归一化；递归入口明确说明 Sitemap 与 SPA DOM 能力。
- 真实 Edge E2E 验证 Sitemap Index、raw gzip 隐藏页、拒绝 HEAD 但允许 GET 页面、SPA 路由、跨域资源、导出下载及五页窄屏界面。

## 1.0.0 - 2026-07-31

- 发布首个稳定正式版；侧栏独立打开时也能识别任意未授权的普通 HTTP/HTTPS 活动网页。
- 增加当前标签页上下文权限；当前页与递归按站点申请，完整实时嗅探仅在用户点击后请求可撤销的 HTTP/HTTPS 全站权限。
- 将快照绑定到每个侧栏所在窗口，避免多个 Edge 窗口互相显示错误网站与任务。
- 完整嗅探覆盖第三方 CDN、无扩展名非 HTML 响应和跨域 frame，同时始终限定在用户指定的当前标签页。
- 在内容端限制标题、URL 和单批资源数量，超大网页不再因整批消息越界而扫描超时。
- 统一图片开关在当前页、实时监听和递归扫描中的行为，并为直达文件应用自定义分类。
- 禁止 `blob:` 临时资源执行无效的打开与元数据探测，任务控制期间禁用重复操作。
- 加强键盘焦点可见性；reduced-motion 下停止无限旋转，继续默认静音且无背景音乐。
- 真实 Edge E2E 覆盖普通网页识别、跨域资源嗅探、按站点扫描、五页窄屏、导出与下载落盘。

## 0.2.3 - 2026-07-31

- 让页面内 `blob:` 临时媒体进入结果库，统一标记为低置信度、临时资源和不可下载。
- 在结果页增加独立“可能资源”分类与数量，默认不再让低置信度发现完全不可见。
- 递归扫描实时显示当前请求 URL 和最近一分钟真实请求数，并淘汰过期速率记录。
- 合并实时扫描进度到活动会话与历史，终态任务不再被迟到的运行事件回退。
- 任务完成、失败或取消后立即移出未完成列表，避免历史页显示陈旧恢复警告。
- 完善按钮按压反馈和 reduced-motion 行为；继续保持默认静音，不播放音效或背景音乐。
- Edge E2E 实际验证 TXT/CSV/JSON、历史导出、下载落盘、可访问名称、24px 按钮目标与历史清空隔离。

## 0.2.2 - 2026-07-30

- 将同标签页任务互斥扩展到跨 Origin 导航，并串行化活动映射写入与启动清理。
- 统一递归任务失败终态，修复异常、暂停检查点失败和后台重启后的陈旧活动映射。
- 仅允许暂停任务恢复并拒绝并发恢复，避免重复爬虫覆盖任务状态。
- 清除本地数据前确认启动中下载已获得浏览器 ID，并在取消失败时保留记录与明确报错。
- 为当前页多 frame 结果增加有限接收窗口，拒绝过期或系统时钟回拨后的旧结果。
- 结果列表响应窗口高度变化，极端元数据卡在 280px/320px/380px 下不再裁切操作区。
- 扩展真实 Edge E2E 的三档窄屏、极端内容、末项遮挡与控制台错误检查；继续默认静音。

## 0.2.1 - 2026-07-29

- 隔离活动标签页快照与会话事件，阻止其他标签页结果污染当前上下文。
- 为同标签页任务增加互斥与安全映射清理，修复旧任务覆盖新任务状态的竞态。
- 从 Edge 下载记录同步真实进度，并在后台恢复和清除全部数据时校准或取消活动任务。
- 为当前页扫描增加超时看门狗，补齐窗口聚焦、导航和 Service Worker 恢复生命周期。
- 保护设置页未保存草稿，补充中文来源/风险文案、单项失败反馈和无障碍状态。
- 修复动态资源属性遗漏、IPv4-mapped IPv6 私网绕过和网络退避无法立即取消。
- 优化 320px/380px 窄侧栏密度、焦点轮廓与响应式布局；继续保持默认无音效和背景音乐。

## 0.2.0 - 2026-07-29

- 修复下载启动阶段取消后被异步结果重新激活的竞态。
- 加固页面扫描结果的标签页与 origin 双重校验。
- 修复历史任务错误恢复入口、删除确认、异常反馈和成功提示样式。
- 统一筛选后的批量选择范围，提示隐藏选择并报告实际入队数量。
- 为无效正则、设置越界、受限页面和异步失败提供明确反馈。
- 完善下载操作状态、进度条、虚拟列表键盘访问和窄侧栏布局。
- 保留 reduced-motion 支持；工具默认不播放音效或背景音乐。

## 0.1.0 - 2026-07-28

- 建立 Manifest V3、React、Vite、TypeScript strict 工程。
- 实现当前页扫描、tab 级实时监听和同源 BFS 递归扫描。
- 实现 URL 安全、文件分类、响应头识别、robots、限速、重试与检查点。
- 实现 IndexedDB、本地历史、权限管理、下载队列和 TXT/CSV/JSON 导出。
- 完成 Edge 侧边栏、Popup、设置页、自动化测试、构建校验和发布 ZIP。
- 加固实时监听的同源导航重注入、跨域跳转和撤权清理。
- 增加网页静态资源识别、历史单次导出/清空、发布密钥审计和跨平台 Edge E2E。
